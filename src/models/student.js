export default class Student {
    constructor(id, firstName, lastName, adress, birthDate, level, img) {
      this.firstName = firstName;
      this.lastName = lastName;
      this.adress = adress;
      this.birthDate = birthDate;
      this.level = level;
      this.img = img;
    }
  }
  